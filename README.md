# SimIR/VCD modern static site

Open `index.html` in a browser or publish this folder to a static web host.

The calculation page validates the basic two-column format in the browser before it opens the external calculator. Replace the endpoint in `assets/app.js` when the final HTTPS endpoint is confirmed.
